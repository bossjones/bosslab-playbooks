#!/usr/bin/env bash

for n in $(kubectl get no | grep -v -i master | grep Ready | awk '{print $1}'| xargs)
do
	echo "k8 label node ${n} with label beta.kubernetes.io/fluentd-ds-ready=true" | highlight
    kubectl label node ${n} beta.kubernetes.io/fluentd-ds-ready=true
done
